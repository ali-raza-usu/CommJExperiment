# The properties set here are used in the build.xml file.
# Change the values as necessary.

srcDir=src
unittestDir=unittest
buildDir=build
libDir=lib
distDir=dist
docDir=docs
javadocDir=javaDoc

common.jar=CharmCommon.jar
classpath=${libDir}/third_party/jboss-j2ee.jar

# Set the value of debug=off if debug information in the classes in not required. 
# Set to off only when production version is compiled.
debug=on
